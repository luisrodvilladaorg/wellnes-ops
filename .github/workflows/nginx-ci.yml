name: Nginx CI - config validation

on:
    push:
        branches:
            - main
            - feature/**
    pull_request:

jobs:
    nginx-ci:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            # CI valida SOLO sintaxis, no infraestructura real
            - name: Validate nginx CI config
              run: |
                  docker run --rm \
                    -v ${{ github.workspace }}/nginx/nginx.ci.conf:/etc/nginx/conf.d/default.conf:ro \
                    nginx:stable-alpine nginx -t
