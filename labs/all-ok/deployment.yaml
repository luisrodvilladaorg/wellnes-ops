apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-dns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-dns
  template:
    metadata:
      labels:
        app: backend-dns
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
          - containerPort: 80